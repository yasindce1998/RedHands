{
  "name": "barzakh_report",
  "description": "Scan a firmware image and generate a detailed report in JSON, HTML, or Markdown format.",
  "binary": "barzakh-scanner",
  "args_template": [
    "report",
    "--target", "{{.target}}",
    "--output", "{{.output}}",
    "--format", "{{.format}}"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "target": {
        "type": "string",
        "description": "Path to firmware image or memory dump to scan and report on"
      },
      "output": {
        "type": "string",
        "description": "Output path for the generated report"
      },
      "format": {
        "type": "string",
        "description": "Report format: json, html, or markdown",
        "enum": ["json", "html", "markdown"],
        "default": "json"
      }
    },
    "required": ["target", "output", "format"]
  }
}
